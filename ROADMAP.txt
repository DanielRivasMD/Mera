add readme
add function to read paths & output yml
mera should be able to load file names as well, for example: const data_csv_file.csv
build mera cli
	using Mera
	ensure(Mera.load())

read .mera.yml by default from cli & from repl => how about creating .mera/ and files names as stems, eg, root.yml
or find an alternative for branches & mirroing

enhancements:
- tempating & copying
- enviroment variable interpolation
- relative root resolution
- integration with FilePathsBase.jl
- listing & visualization
- multi config merge

usefulness:
- lightweight project scaffolding
- ergonomic functions

strengths:
- clean data model (tree)
- ergonomic dot syntax
- flexible input (yaml & toml)
- global default
- clever utilities (ensure & mirror)
- explicit internal field access

weaknesses:
- lack of file support => PRIORITY
- inefficient path navigation => cache
- heavy reliance on dot syntax
- no validation on config semantics
- base path warning only

==================================================
==================================================
